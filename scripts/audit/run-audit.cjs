#!/usr/bin/env node/* eslint-disable no-console *//** * CLAVE SALUD â€” AREA AUDIT RUNNER (v1.3 Optimized) * ðŸ”¬ Ejecuta auditorÃ­as automatizadas y genera reportes versionados. */const fs = require("fs");const path = require("path");const { execSync } = require("child_process");const ROOT = process.cwd();const CONFIG_PATH = path.join(ROOT, "scripts", "audit", "config", "areas.json");const AUDITS_DIR = path.join(ROOT, "docs", "audits");const RESULTS_DIR = path.join(AUDITS_DIR, "results");const PREFLIGHT = require("./preflight.cjs");/** * Parsea argumentos simples --key value o --flag */function parseArgs(argv) {  const args = {};  for (let i = 2; i < argv.length; i += 1) {    const token = argv[i];    if (!token.startsWith("--")) continue;    const key = token.slice(2);    const next = argv[i + 1];    if (!next || next.startsWith("--")) {      args[key] = true;    } else {      args[key] = next;      i += 1;    }  }  return args;}function todayIso() {  const d = new Date();  const yyyy = d.getUTCFullYear();  const mm = String(d.getUTCMonth() + 1).padStart(2, "0");  const dd = String(d.getUTCDate()).padStart(2, "0");  return `${yyyy}-${mm}-${dd}`;}function ensureDir(dirPath) {  fs.mkdirSync(dirPath, { recursive: true });}function sanitizeOutput(raw = "") {  // Limpiar caracteres de control y normalizar saltos de lÃ­nea  return String(raw).replace(/\r\n/g, "\n").trim();}/** * Ejecuta un comando sÃ­ncrono capturando salida y errores. */function runCommand(command) {  const startedAt = new Date().toISOString();  console.log(`\nðŸš€ [Audit] Ejecutando: ${command}`);  try {    const stdout = execSync(command, {      cwd: ROOT,      stdio: ["ignore", "pipe", "pipe"],      encoding: "utf8",      maxBuffer: 1024 * 1024 * 16, // 16MB buffer para logs extensos    });    return {      command,      status: "pass",      startedAt,      finishedAt: new Date().toISOString(),      output: sanitizeOutput(stdout),      error: "",    };  } catch (error) {    return {      command,      status: "fail",      startedAt,      finishedAt: new Date().toISOString(),      output: sanitizeOutput(error.stdout || ""),      error: sanitizeOutput(error.stderr || error.message || "Error desconocido"),    };  }}function summarize(results) {  const pass = results.filter((r) => r.status === "pass").length;  const fail = results.filter((r) => r.status === "fail").length;  const warning = results.filter((r) => r.status === "warning").length;  const overall = fail > 0 ? "ALTO RIESGO" : warning > 0 ? "RIESGO MEDIO" : "SIN BLOQUEOS";  return { pass, fail, warning, overall };}/** * Genera el cuerpo del reporte en Markdown */function toMarkdown({ areaKey, config, date, results, summary, reportPath }) {  const lines = [    `# AuditorÃ­a automÃ¡tica â€” ${config.title}`,    "",    `- **Ãrea:** \`${areaKey}\``,    `- **Fecha ejecuciÃ³n:** \`${date}\``,    `- **Generado por:** \`scripts/audit/run-audit.cjs\``,    `- **Estado general:** **${summary.overall}**`,    "",    "## Resumen de resultados",    "",    `- âœ… Pass: **${summary.pass}**`,    `- âŒ Fail: **${summary.fail}**`,    `- âš ï¸ Warning: **${summary.warning}**`,    "",    "## Alcance del Ã¡rea",    "",    config.description,    "",    "### Puntos de foco",    ...(config.focus || []).map((item) => `- ${item}`),    "",    "### Evidencia revisada",    ...(config.evidenceFiles || []).map((file) => `- \`${file}\``),    "",    "## Ejecuciones",    "",    "| Estado | Comando | Inicio | Fin |",    "|---|---|---|---|",    ...results.map((r) => {      const icon = r.status === "pass" ? "âœ…" : r.status === "warning" ? "âš ï¸" : "âŒ";      return `| ${icon} ${r.status.toUpperCase()} | \`${r.command}\` | ${r.startedAt} | ${r.finishedAt} |`;    }),    "",    "## Detalle de salida",    "",  ];  for (const r of results) {    const icon = r.status === "pass" ? "âœ…" : r.status === "warning" ? "âš ï¸" : "âŒ";    lines.push(`### ${icon} ${r.command}`);    lines.push("");    if (r.output) {      lines.push("```text");      // LÃ­mite de 20,000 para no truncar resultados de tests complejos      lines.push(        r.output.length > 20000          ? r.output.slice(0, 20000) + "\n\n[... Salida truncada por longitud ...]"          : r.output      );      lines.push("```");    } else {      lines.push("_Sin salida estÃ¡ndar._");    }    if (r.error) {      lines.push("\n**Error/Stderr:**\n");      lines.push("```text");      lines.push(r.error.slice(0, 10000));      lines.push("```");    }    lines.push("");  }  lines.push("## Hallazgos manuales (Post-AuditorÃ­a)");  lines.push("\n- [ ] H-001 (Pendiente)");  lines.push("- [ ] H-002 (Pendiente)");  lines.push("");  lines.push(`- **Reporte:** \`${path.relative(ROOT, reportPath)}\``);  lines.push(`- **Timestamp:** \`${new Date().toISOString()}\``);  return lines.join("\n");}function updateIndex() {  ensureDir(AUDITS_DIR);  const areaFiles = [];  function walk(dir) {    const entries = fs.readdirSync(dir, { withFileTypes: true });    for (const entry of entries) {      if (entry.name === "README.md" || entry.name === "TEMPLATE.md") continue;      const full = path.join(dir, entry.name);      if (entry.isDirectory()) {        walk(full);      } else if (entry.isFile() && entry.name.endsWith(".md")) {        const rel = path.relative(AUDITS_DIR, full).replace(/\\/g, "/");        if (rel.startsWith("results/")) continue;        areaFiles.push(rel);      }    }  }  walk(AUDITS_DIR);  areaFiles.sort((a, b) => b.localeCompare(a));  const lines = [    "# ðŸ“‘ Ãndice de AuditorÃ­as",    "",    "Generado automÃ¡ticamente por el corredor de auditorÃ­a.",    "",    "| Reporte | Fecha ModificaciÃ³n |",    "|---|---|",    ...areaFiles.map((rel) => {      const full = path.join(AUDITS_DIR, rel);      const stat = fs.statSync(full);      return `| [${rel}](${rel}) | ${stat.mtime.toISOString()} |`;    }),    ""  ];  fs.writeFileSync(path.join(AUDITS_DIR, "README.md"), lines.join("\n"), "utf8");}function persistJsonResult({ areaKey, date, config, results, summary, reportPath }) {  ensureDir(RESULTS_DIR);  const file = path.join(RESULTS_DIR, `${date}_${areaKey}.json`);  const payload = {    area: areaKey,    title: config.title,    date,    generatedAt: new Date().toISOString(),    summary,    reportPath: path.relative(ROOT, reportPath).replace(/\\/g, "/"),    results,  };  fs.writeFileSync(file, JSON.stringify(payload, null, 2), "utf8");}function runAreaAudit({ areaKey, config, date, profile }) {  const areaConfig = config[areaKey];  if (!areaConfig) {    console.error(`âŒ Ãrea desconocida: ${areaKey}.`);    return null;  }  const reportDir = path.join(AUDITS_DIR, date.split("-")[0]);  const reportPath = path.join(reportDir, `${date}_${areaKey}_${profile}.md`);  ensureDir(reportDir);  const commands = Array.isArray(areaConfig.profiles[profile]) ? areaConfig.profiles[profile] : [];  const results = [];  for (const cmd of commands) {    results.push(runCommand(cmd));  }  const summary = summarize(results);  const markdown = toMarkdown({ areaKey, config: areaConfig, date, results, summary, reportPath });  fs.writeFileSync(reportPath, markdown, "utf8");  persistJsonResult({ areaKey, date, config: areaConfig, results, summary, reportPath });    return { areaKey, summary, reportPath };}function main() {  if (!fs.existsSync(CONFIG_PATH)) {    console.error(`âŒ No se encontrÃ³ configuraciÃ³n en: ${CONFIG_PATH}`);    process.exit(1);  }  const config = JSON.parse(fs.readFileSync(CONFIG_PATH, "utf8"));  const args = parseArgs(process.argv);  if (args.help || (!args.area && !args.all)) {    console.log("Uso: node scripts/audit/run-audit.cjs [--area <id> | --all] [--profile smoke|deep] [--require-playwright]");    process.exit(0);  }  const date = String(args.date || todayIso());  const profile = String(args.profile || "smoke").toLowerCase();    console.log(`\nðŸ› ï¸  [Audit] Perfil seleccionado: ${profile.toUpperCase()}`);    const entries = [];  if (args.all) {    console.log("ðŸš€ [Orquestador] Ejecutando auditorÃ­a COMPLETA...");        // Preflight Global    PREFLIGHT.checkEnv();    PREFLIGHT.checkPlaywright();    const keys = Object.keys(config).sort();    for (const key of keys) {      console.log(`---------- Ãrea: ${key} ----------`);      const res = runAreaAudit({ areaKey: key, config, date, profile });      if (res) entries.push(res);    }  } else {    // Preflight Single Area    const areaKey = String(args.area).trim();    const areaConfig = config[areaKey];    if (areaConfig) {      const requiresPW = areaConfig.profiles[profile].some(cmd => cmd.includes("playwright"));      if (requiresPW) {        PREFLIGHT.checkEnv();        PREFLIGHT.checkPlaywright();      }    }    const res = runAreaAudit({ areaKey, config, date, profile });    if (res) entries.push(res);  }  updateIndex();  const totalFail = entries.reduce((acc, e) => acc + e.summary.fail, 0);  const totalPass = entries.reduce((acc, e) => acc + e.summary.pass, 0);  const totalWarn = entries.reduce((acc, e) => acc + e.summary.warning, 0);  if (args.all) {    const consolidatedPath = path.join(RESULTS_DIR, `${date}_multiagente_consolidado.json`);    const consolidated = {      date,      generatedAt: new Date().toISOString(),      summary: { pass: totalPass, fail: totalFail, warning: totalWarn },      details: entries    };    fs.writeFileSync(consolidatedPath, JSON.stringify(consolidated, null, 2), "utf8");    console.log("\n==========================================");    console.log(`ðŸ AuditorÃ­a Consolidada Finalizada`);    console.log(`Fails Totales: ${totalFail}`);    if (totalFail === 0) {      console.log("âœ… Gate clean-audit: PASS (Sin bloqueos detectados)");    } else {      console.error("âŒ Gate clean-audit: FAIL (Se requiere una suite limpia)");    }    console.log("==========================================");  }  if (totalFail > 0) process.exit(2);}main();
